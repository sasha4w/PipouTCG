{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Dans dist/ pour être supprimé avec lui (deleteOutDir) : sinon tsc
    // croit le build à jour et n'émet qu'une partie des fichiers.
    "tsBuildInfoFile": "./dist/tsconfig.build.tsbuildinfo"
  },
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"]
}
